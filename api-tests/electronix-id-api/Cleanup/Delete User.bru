meta {
  name: Delete User
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/users/{{userId}}
  auth: inherit
}

assert {
  res.status: eq 204
}

docs {
  Removes the user created in the Users folder.
}
