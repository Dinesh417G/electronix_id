meta {
  name: Restore Version 1
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/documents/{{documentId}}/versions/1/restore
  auth: inherit
}

assert {
  res.status: eq 201
}

docs {
  Makes a NEW highest version copying v1. Nothing is deleted; lineage preserved.
}
