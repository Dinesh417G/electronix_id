meta {
  name: List Machines
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/machines?page=1&per_page=20
  auth: inherit
}

params:query {
  page: 1
  per_page: 20
}

assert {
  res.status: eq 200
  res.body.data: isDefined
}

docs {
  Paginated. Envelope: { data, page, per_page, total }.
}
